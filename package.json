{
  "name": "auto-pilot-deploy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "pnpm run start:server && pnpm run start:client",
    "build": "pnpm run build:server && pnpm run build:client",
    "build:client": "pnpm --filter @pilot/client build",
    "build:server": "pnpm --filter @pilot/server build",
    "start:client": "pnpm --filter @pilot/client start",
    "start:server": "pnpm --filter @pilot/server start",
    "prepare": "husky install",
    "commitlint": "commitlint -env HUSKY_GIT_PARAMS --config .lint/config/commitlint.config.json"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "socket.io": "^4.6.1"
  },
  "devDependencies": {
    "@commitlint/cli": "8.3.5",
    "@commitlint/config-conventional": "8.3.4",
    "@commitlint/types": "^17.4.4",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.3",
    "eslint": "^8.37.0",
    "husky": "^8.0.0",
    "lint-staged": "^13.2.0",
    "prettier": "^2.0.5",
    "stylelint": "^15.3.0",
    "stylelint-scss": "^4.6.0"
  },
  "lint-staged": {
    "packages/**/*.{ts,tsx,js,json}": [
      "eslint --config .lint/config/.eslintrc.json --fix"
    ],
    "*.{md,json,yml}": [
      "prettier --trailing-comma es5 --single-quote --write"
    ],
    "packages/**/*.scss": [
      "stylelint --config .lint/config/.stylelintrc.json"
    ]
  }
}
